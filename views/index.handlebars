<style>
    .link {
        height: 55px;
    }

    .updatedArticles {
        text-align: center;
    }

    .whiteTitle {
        color: rgb(247, 179, 34);
        padding-left: 10px;
        padding-right: 10px;
        font-weight: bolder;
    }
</style>
<h1 class="updatedArticles">Updated Articles</h1>
<div class="container center-align">
    {{#each articles}}
    <div class="col s12 m12">
    <div class="container">
        <div class="container">
            <div class="row">
                <div class="card">
                    <div class="card-image">
                        <img src="{{img}}">
                        <a class="btn-floating halfway-fab waves-effect waves-light red"><i class="material-icons">add</i></a>
                    </div>
                    <div class="center-align">
                        <h1 class="card-title whiteTitle">{{title}}</h1>
                    </div>
                    <div class="card-content">
                        <p class="left-align">{{summary}}</p>
                    </div>
                    <div class="card-action link">
                        <a href="{{link}}" class="left">This is a link</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </div>
    {{/each}}
</div>